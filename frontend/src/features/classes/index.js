/**
 * Classes Feature Module
 * Barrel export for clean imports
 */

// Pages
export { ClassDetailPage } from './pages';

// Components (for reuse in other modules)
export { 
  Avatar, 
  TabButton, 
  Toast 
} from './components';

// Hooks (for reuse in other modules)
export { 
  useToast 
} from './hooks';
